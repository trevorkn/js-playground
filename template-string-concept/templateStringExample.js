// Declare a constant variable to store the blog title
const title = 'Best reads of 2019';

// Declare a constant variable to store the author's name
const author = 'Mario';

// Declare a constant variable to store the number of likes
const likes = 30;

// Use a template literal to create a string that includes the above variables
let result = `The blog called ${title} by ${author} has ${likes} likes.`;

// Output the result to the console
console.log(result);
